# 로또 번호 생성기 프로젝트

## 개요

이 프로젝트는 HTML, CSS, JavaScript를 사용하여 로또 번호 생성기를 만듭니다. 사용자는 버튼 클릭으로 1부터 45까지의 숫자 중 6개의 고유한 랜덤 번호를 생성하고, 시각적으로 아름답게 표시된 결과를 확인할 수 있습니다. 다크 모드와 라이트 모드 전환 기능을 제공합니다.

## 디자인 및 기능

*   **헤더:** "Lotto Number Generator" 라는 제목을 표시합니다.
*   **테마 전환:** 화면 상단에 다크 모드와 라이트 모드를 전환할 수 있는 버튼을 제공합니다.
*   **번호 표시 영역:** 한 번에 5개의 로또 번호 세트를 생성하여 표시합니다. 각 세트는 6개의 고유한 랜덤 번호가 공 모양 아이콘 안에 담겨 일렬로 배열됩니다.
*   **생성 버튼:** "Generate 5 Sets" 버튼을 클릭하면 새로운 5개 세트의 로또 번호를 생성합니다.
*   **디자인:**
    *   **다크 모드:** 어두운 배경(#1a1a1a)과 밝은 텍스트를 사용하며, 네온 효과를 강조합니다.
    *   **라이트 모드:** 밝은 배경(#f0f2f5)과 어두운 텍스트를 사용하며, 깔끔하고 정돈된 느낌을 줍니다.
    *   생성된 번호는 각각 다른 색상의 공 안에 표시되어 시각적 구분을 줍니다.
    *   세트 간에는 적절한 간격을 두어 구분하기 쉽게 만듭니다.
    *   버튼에 호버 효과와 그림자 효과를 주어 상호작용성을 높입니다.
    *   모바일과 데스크톱 환경 모두에 최적화된 반응형 레이아웃을 가집니다.

## 현재 구현 계획

1.  **HTML (`index.html`):**
    *   테마 전환 버튼과 웹 컴포넌트인 `<lotto-generator>`를 포함합니다.

2.  **CSS (`style.css`):**
    *   다크 모드와 라이트 모드를 위한 CSS 변수를 정의합니다 (`:root` 및 `[data-theme="light"]`).
    *   전역 배경색과 텍스트 색상을 변수에 따라 적용합니다.

3.  **JavaScript (`main.js`):**
    *   테마 전환 로직을 구현합니다 (localStorage를 사용하여 선호도 저장).
    *   `LottoGenerator` 클래스의 내부 스타일을 CSS 변수를 사용하도록 수정하여 테마 전환에 반응하게 합니다.
    *   기존의 5개 세트 생성 및 렌더링 로직을 유지합니다.

4.  **GitHub 업로드:**
    *   최종 코드를 `https://github.com/michung202-ai/shorts-ai` 저장소에 업로드합니다.
