# 로또 번호 생성기 프로젝트

## 개요

이 프로젝트는 HTML, CSS, JavaScript를 사용하여 로또 번호 생성기를 만듭니다. 사용자는 버튼 클릭으로 1부터 45까지의 숫자 중 6개의 고유한 랜덤 번호를 생성하고, 시각적으로 아름답게 표시된 결과를 확인할 수 있습니다.

## 디자인 및 기능

*   **헤더:** "Lotto Number Generator" 라는 제목을 표시합니다.
*   **번호 표시 영역:** 한 번에 5개의 로또 번호 세트를 생성하여 표시합니다. 각 세트는 6개의 고유한 랜덤 번호가 공 모양 아이콘 안에 담겨 일렬로 배열됩니다.
*   **생성 버튼:** "Generate 5 Sets" 버튼을 클릭하면 새로운 5개 세트의 로또 번호를 생성합니다.
*   **디자인:**
    *   전체적으로 어두운 테마 배경에 네온 색상의 강조 효과를 사용합니다.
    *   생성된 번호는 각각 다른 색상의 공 안에 표시되어 시각적 구분을 줍니다.
    *   세트 간에는 적절한 간격을 두어 구분하기 쉽게 만듭니다.
    *   버튼에 호버 효과를 주어 상호작용성을 높입니다.
    *   모바일과 데스크톱 환경 모두에 최적화된 반응형 레이아웃을 가집니다.

## 현재 구현 계획

1.  **HTML (`index.html`):**
    *   웹 컴포넌트인 `<lotto-generator>`를 유지합니다.

2.  **CSS (`style.css`):**
    *   기존 스타일을 유지하며, 다중 세트 표시에 적합하도록 보정합니다.

3.  **JavaScript (`main.js`):**
    *   `LottoGenerator` 클래스의 `generateNumbers` 메서드를 수정하여 5개의 세트를 생성하도록 변경했습니다.
    *   `renderNumbers` 메서드를 수정하여 5개의 세트를 각각 행 단위로 렌더링하도록 변경했습니다.
    *   웹 컴포넌트 내부 스타일을 다중 세트 레이아웃에 맞춰 업데이트했습니다.

4.  **GitHub 업로드:**
    *   최종 코드를 `https://github.com/michung202-ai/shorts-ai` 저장소에 업로드합니다.
